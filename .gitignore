# Virtual environment
.venv/

# Generated output (dashboards, logs, history, pinned cert)
output/

# Secrets — localhost-only plaintext fallback; never commit
credentials.txt

# Machine-specific real configuration (paths, tariff history); never commit
config.toml

# Personal bill data for reconciliation (roadmap item 29); never commit
bills.csv

# Personal battery lifecycle notes (roadmap item 26); never commit
annotations.csv

# Local source backups
backup/

# Python caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor/OS cruft
.DS_Store
Thumbs.db

# Local Claude Code permission overrides (machine-specific)
.claude/settings.local.json

# Local planning scratch artifacts (agent-native visual plans)
.agent-native/

# Subagent-driven-development scratch (task briefs, reports, progress ledger)
.superpowers/
